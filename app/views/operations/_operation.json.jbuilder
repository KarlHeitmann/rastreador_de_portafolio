json.extract! operation, :id, :activo, :broker, :fecha_entrada, :precio_de_compra, :unidades_compradas, :stop_loss, :posicion, :status, :comentario, :wallet_id, :created_at, :updated_at
json.url operation_url(operation, format: :json)
